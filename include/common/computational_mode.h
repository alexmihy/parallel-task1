#pragma once

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

enum ComputationalMode
{
	USE_SEQUENTIAL_MODE,
	USE_CPU_MODE,
	USE_GPU_MODE,
	USE_HYBRID_MODE,
    USE_KNL_MODE
};

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
